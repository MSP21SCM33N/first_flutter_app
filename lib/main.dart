import 'package:flutter/material.dart'
name = 'Max';

main () {

}
class MyApp extends StatelessWidget{ // receives all the features from another class
  build (context) {
    return MaterialApp(); // Root widget
  }
}